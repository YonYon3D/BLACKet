let name = prompt("Type the box (Example: Add Tokens)"); let amt = null; let i = 1; if (confirm("Choose the amount of box?\nOk - Yes\nCancel - No")) { amt = Number(prompt("Choose the amount of boxes")); } else { amt = 99999999999999999999999; } function buyBox() { var postData = 'box=' + name; $.post('/worker/box/openbox.php', postData, function(data) { var myArray = data.split("|"); var word = myArray[0]; console.log(word); if (data === "Moaning Bot") { i--; } }); } var check = setInterval(() => { if (i <= amt) { buyBox(); i++; } else { clearInterval(check); alert("No Error Occurpted. Spam open box completed!"); } }, 751);
